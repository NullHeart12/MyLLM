# torch / torchvision / torchaudio 必须单独从 PyTorch 官方源安装：
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
transformers
tokenizers
datasets
swanlab
modelscope
huggingface_hub
tqdm
bitsandbytes
peft
accelerate
numpy               # 代码直接 import,torch 装时已带,显式声明更稳
pyarrow             # deal_dataset 用了 pyarrow.compute,datasets 装时已带
mlflow
opencc-python-reimplemented   # ChinesePoetryConverter 把唐诗繁体转简体用